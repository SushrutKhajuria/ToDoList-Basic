
// using comonent and props
// this is a component , called user , which will be called in the
// above example

export const Planet = (props)=>{
    return(
      <div>
        {props.name}
        
      </div>
    )
  
  }
  
  